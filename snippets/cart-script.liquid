<script>

  var $ = jQuery;
  jQuery(document).ready(function($) {

// your stuff
    $(document).on('click', '.add-to-cart', function(e) {
      e.preventDefault();

      var variantId = this.getAttribute('data-variant-id');
      var productId = this.getAttribute('data-product-id');
      var quantity = 1; // You can adjust this to allow the user to specify the quantity.

      Shopify.addItem(productId, 1, variantId, function(cart) {

// Callback function - handle success or error
        console.log('Item added to cart:', cart);

// You can update the cart UI or show a notification here
      });
    })
  });
</script>